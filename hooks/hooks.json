{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,re; d=json.load(sys.stdin); c=d.get('tool_input',{}).get('command',''); bad=re.search(r'git\\\\s+commit[^|;&]*--no-verify|git\\\\s+config[^|;&]*hooksPath', c); sys.exit(2 if bad else 0)\""
          }
        ]
      }
    ]
  }
}
